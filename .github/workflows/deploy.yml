name: Deploy to cPanel

# Auto-deploy over FTPS. (Namecheap shared hosting has SSH/shell access
# disabled, so we upload the site files via FTP instead of pulling on the
# server.) Runs on every push to main, or manually from the Actions tab.
#
# Required repository secrets (Settings -> Secrets and variables -> Actions):
#   FTP_SERVER    e.g. server125.web-hosting.com
#   FTP_USERNAME  the MAIN cPanel account user, e.g. meezhutu
#   FTP_PASSWORD  that account's password
# server-dir is public_html/ because the main account's FTP root is the
# home directory (/home/<user>), and the site lives in public_html.

on:
  push:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: cpanel-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Upload site to cPanel over FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          local-dir: ./
          server-dir: public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            build.py
            _extract_map.py
            **/*.zip
